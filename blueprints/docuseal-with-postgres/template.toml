[variables]
main_domain = "${domain}"

[config]

[[config.domains]]
serviceName = "docuseal"
port = 3000
host = "${main_domain}"

[config.env]
SERVICE_URL_DOCUSEAL = "https://${main_domain}"
SERVICE_USER_POSTGRES = "${username}"
SERVICE_PASSWORD_POSTGRES = "${password:32}"
