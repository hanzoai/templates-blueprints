[variables]
main_domain = "${domain}"

[config]

[[config.domains]]
serviceName = "hatchet-dashboard"
port = 80
host = "${main_domain}"

[config.env]
SERVICE_USER_POSTGRES = "${username}"
SERVICE_PASSWORD_POSTGRES = "${password:32}"
SERVICE_USER_RABBITMQ = "${username}"
SERVICE_PASSWORD_RABBITMQ = "${password:32}"
