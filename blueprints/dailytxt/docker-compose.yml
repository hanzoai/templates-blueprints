services:
  dailytxt:
    image: phitux/dailytxt:2.6.2
    environment:
      SECRET_KEY: ${DAILYTXT_SECRET_KEY}
      ALLOW_REGISTRATION: ${DAILYTXT_ALLOW_REGISTRATION}
      DATA_INDENT: "2"
      LOGOUT_AFTER_DAYS: "60"
      ADMIN_PASSWORD: ${DAILYTXT_ADMIN_PASSWORD}
    volumes:
      - dailytxt-data:/data/
    expose:
      - "80"
    restart: always
volumes:
  dailytxt-data: {}
