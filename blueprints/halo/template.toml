[variables]
main_domain = "${domain}"

[config]

[[config.domains]]
serviceName = "halo"
port = 8092
host = "${main_domain}"

[config.env]
HALO_DATABASE_PASSWORD = "${hash:32}"
HALO_EXTERNAL_URL = "https://${main_domain}"
