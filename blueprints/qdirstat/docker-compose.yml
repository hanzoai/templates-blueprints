services:
  qdirstat:
    image: lscr.io/linuxserver/qdirstat:1.8.1-ls82
    volumes:
      - qdirstat-config:/config
      - qdirstat-data:/data
      - qdirstat-host:/host
    expose:
      - "3000"
    restart: always
volumes:
  qdirstat-config: {}
  qdirstat-data: {}
  qdirstat-host: {}
