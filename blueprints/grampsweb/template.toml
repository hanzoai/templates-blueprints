[variables]
main_domain = "${domain}"
version = "26.5.3"
redis-version = "7.2.13-alpine"
secret-key = "${password:32}"

[config]

[[config.domains]]
serviceName = "grampsweb"
port = 5000
host = "${main_domain}"

[config.env]
VERSION = "${version}"
REDIS_VERSION = "${redis-version}"
SECRET_KEY = "${secret-key}"
