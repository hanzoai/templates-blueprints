[variables]
main_domain = "${domain}"

[config]

[[config.domains]]
serviceName = "umami-analytics"
port = 8153
host = "${main_domain}"

[config.env]
UMAMI_ANALYTICS_DB_PASSWORD = "${hash:32}"
UMAMI_ANALYTICS_APP_SECRET = "${hash:32}"
