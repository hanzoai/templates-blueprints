services:
  jdownloader2:
    image: jlesage/jdownloader-2:latest
    environment:
      - USER_ID=$PUID
      - GROUP_ID=$PGID
      - TZ=$TZ
      - MYJDOWNLOADER_EMAIL=$MYJDOWNLOADER_EMAIL
      - MYJDOWNLOADER_PASSWORD=$MYJDOWNLOADER_PASSWORD
      - MYJDOWNLOADER_DEVICE_NAME=$MYJDOWNLOADER_DEVICE_NAME
      - JDOWNLOADER_HEADLESS=$JDOWNLOADER_HEADLESS
    expose:
      - "5800"
      - "5900"
      - "3129"
    volumes:
      - jdownloader2-config:/config
      - jdownloader2-output:/output
    restart: unless-stopped
volumes:
  jdownloader2-config: {}
  jdownloader2-output: {}
