[variables]
main_domain = "${domain}"

[config]

[[config.domains]]
serviceName = "unleash"
port = 4242
host = "${main_domain}"

[config.env]
SERVICE_URL_UNLEASH = "https://${main_domain}"
SERVICE_PASSWORD_UNLEASH = "${password:32}"
