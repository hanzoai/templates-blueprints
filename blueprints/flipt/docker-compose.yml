services:
  flipt:
    image: docker.flipt.io/flipt/flipt:latest
    volumes:
      - flipt-data:/var/opt/flipt
    healthcheck:
      test:
        - CMD
        - wget
        - --spider
        - --quiet
        - http://127.0.0.1:8080
      interval: 2s
      timeout: 10s
      retries: 15
