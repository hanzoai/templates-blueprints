[variables]
main_domain = "${domain}"
version = "0.1.17"
tz = "America/Los_Angeles"

[config]

[[config.domains]]
serviceName = "databag"
port = 7000
host = "${main_domain}"

[config.env]
VERSION = "${version}"
TZ = "${tz}"
