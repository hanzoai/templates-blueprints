[variables]
main_domain = "${domain}"

[config]

[[config.domains]]
serviceName = "gitea"
port = 3000
host = "${main_domain}"

[config.env]
SERVICE_USER_POSTGRESQL = "${username}"
SERVICE_PASSWORD_POSTGRESQL = "${password:32}"
