[variables]
main_domain = "${domain}"

[config]

[[config.domains]]
serviceName = "joomla"
port = 80
host = "${main_domain}"

[config.env]
SERVICE_USER_JOOMLA = "${username}"
SERVICE_PASSWORD_JOOMLA = "${password:32}"
SERVICE_PASSWORD_ROOT = "${password:32}"
