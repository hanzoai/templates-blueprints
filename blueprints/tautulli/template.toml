[variables]
main_domain = "${domain}"
version = "v2.7.5"
tz = "America/Los_Angeles"
puid = "1000"
guid = "1000"

[config]

[[config.domains]]
serviceName = "tautulli"
port = 8181
host = "${main_domain}"

[config.env]
VERSION = "${version}"
TZ = "${tz}"
PUID = "${puid}"
GUID = "${guid}"
