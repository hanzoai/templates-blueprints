[variables]
main_domain = "${domain}"
version = "2.5"
postgres-version = "14.1-alpine"
db-pass = "daTaBaSePaSsWorD"

[config]

[[config.domains]]
serviceName = "wiki-js"
port = 3000
host = "${main_domain}"

[config.env]
VERSION = "${version}"
POSTGRES_VERSION = "${postgres-version}"
DB_PASS = "${db-pass}"
