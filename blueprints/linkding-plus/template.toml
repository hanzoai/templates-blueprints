[variables]
main_domain = "${domain}"

[config]

[[config.domains]]
serviceName = "linkding-plus"
port = 9090
host = "${main_domain}"

[config.env]
SERVICE_USER_LINKDING = "${username}"
SERVICE_PASSWORD_LINKDING = "${password:32}"
