services:
  grav:
    image: lscr.io/linuxserver/grav:1.8.0
    environment:
      PUID: "1000"
      PGID: "1000"
      TZ: ${TZ}
    volumes:
      - grav-config:/config
    expose:
      - "80"
    restart: always
volumes:
  grav-config: {}
