services:
  peanut:
    image: brandawg93/peanut:6.0.0
    environment:
      WEB_PORT: 8080
    expose:
      - "8080"
    volumes:
      - peanut-config:/config
    restart: unless-stopped
volumes:
  peanut-config: {}
