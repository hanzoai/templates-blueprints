services:
  vert:
    image: ghcr.io/vert-sh/vert:latest
    environment:
      - PUB_VERT_URL=${SERVICE_URL_VERT_80}
      - PUB_HOSTNAME=${SERVICE_URL_VERT_80}
      - PUB_PORT=80
      - PUB_ENV=production
