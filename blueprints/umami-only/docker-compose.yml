services:
  umami-only:
    image: ghcr.io/umami-software/umami:${UMAMI_VERSION}
    environment:
      DATABASE_URL: postgresql://${DB_USER}:${DB_PASS}@${DB_URL}/${DB_NAME}
    expose:
      - "3000"
    restart: always
