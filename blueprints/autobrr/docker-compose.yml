services:
  autobrr:
    image: ghcr.io/autobrr/autobrr:v1.79.0
    environment:
      TZ: $TZ
    expose:
      - "7474"
    volumes:
      - autobrr-config:/config
    restart: unless-stopped
volumes:
  autobrr-config: {}
