# Sync-notice stub — the canonical deploy pipeline is native to Hanzo Git:
#   .hanzo/workflows/deploy.yml (in-cluster act_runner → BuildKit →
#   ghcr.io/hanzoai/templates-blueprints:<sha> → kubectl patch app
#   templates-blueprints → operator reconcile → hanzocd).
# GitHub is a mirror. This workflow builds/deploys NOTHING; it exists only so a
# manual dispatch surfaces where the real pipeline lives.
name: CI/CD

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Native pipeline notice
        run: echo "native pipeline is .hanzo/workflows/deploy.yml; GitHub is a mirror"
